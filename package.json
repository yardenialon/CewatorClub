{
  "name": "simpliigood-creator-club",
  "version": "0.1.0",
  "private": true,
  "description": "SimpliiGood Creator Club - local, bilingual (HE/EN) prototype for managing a creator & ambassador community in Israel and the US.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:check": "node scripts/build.mjs --check",
    "serve": "node scripts/serve.mjs",
    "test": "node --test test/core.test.mjs",
    "test:ui": "node --test test/ui.test.mjs",
    "test:all": "npm run build:check && npm test && npm run test:ui"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
