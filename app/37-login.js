/* Sign-in screen (remote mode only): request a one-time link by e-mail. */
function renderLogin(){
 document.documentElement.dir=lang==='he'?'rtl':'ltr';
 const sent='<div class="mini-info"><strong>'+e(t('linkSent'))+'</strong><br><bdi>'+e(linkSentTo)+'</bdi>'+(devLink?'<p class="small" style="margin:12px 0 8px">'+e(t('devLinkNote'))+'</p><a class="btn sun" id="dev-link" href="'+e(devLink)+'">'+e(t('openDevLink'))+'</a>':'')+'</div><div class="modal-actions">'+btn('tryAnotherEmail','loginAgain','','secondary')+'</div>';
 const form='<form id="login-form">'+field('email','email','','email','required maxlength="254" autocomplete="email" placeholder="creator@example.test"')+'<div id="login-errors" class="inline-error hide" role="alert"></div><div class="modal-actions"><button type="submit" class="btn sun">'+e(t('sendLink'))+'</button>'+btn('signup','apply','','secondary')+'</div></form><p class="tiny muted" style="margin-top:14px">'+e(t('loginHelp'))+'</p>';
 $('#app').innerHTML='<div class="content onboarding"><header class="onboarding-header"><div class="brandmark">Simplii<span>Good</span></div><div class="brand-sub" style="margin:12px 0 20px">CREATOR CLUB</div><button class="lang-btn" data-action="lang">'+(lang==='he'?'EN':'HE')+'</button></header><div class="page-title"><div><h1>'+e(t('loginTitle'))+'</h1><p>'+e(t('loginSub'))+'</p></div></div><section class="card">'+(linkSentTo?sent:form)+'</section></div>';
}
